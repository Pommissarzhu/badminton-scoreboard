import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.badmintonscore',
  appName: 'Badminton Score',
  webDir: 'build'
};

export default config;
